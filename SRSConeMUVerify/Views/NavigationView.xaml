<UserControl x:Class="SRSConeMUVerify.Views.NavigationView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:SRSConeMUVerify.Views"
             xmlns:icons="http://metro.mahapps.com/winfx/xaml/iconpacks"
             mc:Ignorable="d" 
             d:DesignHeight="150" d:DesignWidth="1200">
    <Grid>
      <StackPanel HorizontalAlignment="Right" Orientation="Horizontal">
      <Button Margin="5" Height="50" Width="50"  Foreground="#16499A"  Command="{Binding PrintView}">
         <Button.ToolTip>
            <ToolTip>
               Save as PDF
            </ToolTip>
         </Button.ToolTip>
         <icons:FontAwesome Kind="FilePdfRegular" Height="30" Width="30"/>
      </Button>
         <Button Margin="5" Height="50" Width="50" Foreground="#16499A" Command="{Binding LaunchConfigurationCommand}">
         <Button.ToolTip>
            <ToolTip>
               Configure
            </ToolTip>
         </Button.ToolTip>
            <icons:FontAwesome Kind="CogSolid" Height="30" Width="30"/>
        </Button>
      </StackPanel>
   </Grid>
</UserControl>
